# Session 025

Date:
2026-09-16

## Goal

Complete the **explicitly authorized production backend deployment** for Sprint-007 at `2fa0052`: verify the production configuration, confirm the pre-deploy state, and (as executed by the operator) apply the two schema migrations; then verify production end-to-end, record the deployment evidence, and stop at the post-deploy browser-smoke step.

---

## Completed

- Verified the deployed Angular configuration against `src/services/service.ts`: deployed `Constants` (in `chunk-LHXZHLLH.js`) is identical — `ApiBase = 'https://app.rbttacesd.co.za/api/'` on non-localhost, `http://localhost:8080/` only on localhost (0 occurrences of `localhost:8080` in the deployed bundle). Web path resolves `/api/api-nodes/...`; `config/*.php` are executed, not leaked. CORS correct (allowed origin echoed + credentials; preflight 200).
- Established the production path mapping from the FileZilla log: server root folder `/app.rbttacesd.co.za`; API at `/api/api-nodes/...` (no `/api/api/` nesting).
- **Diagnosed the pre-deploy blocker:** production DB `rbttaces_api` was **missing the Sprint-007 tables**. Authenticated SA calls returned `SQLSTATE[42S02] ... Table 'rbttaces_api.metric_type_accounts' doesn't exist` and `... achievements doesn't exist`, while existing-table endpoints returned `200`. Code (backend + frontend) had been whole-folder-dropped ahead of schema.
- Operator ran the two migrations in order:
  1. `api-incubator-os/migrations/2026-09-15-results-achievements.sql`
  2. `api-incubator-os/migrations/2026-09-16-achievements-snapshot-guard.sql`
- **Post-migration verification (authenticated, live `app.rbttacesd.co.za`)** — all green; see below.
- Prepared the corrected 30-file manifest (13 achievement endpoints + 3 evidence + 5 target/measurement + 5 account-binding + 3 models + 1 service; 8 modified; 2 deleted; 2 SQL) and a SHA-256 integrity manifest at `2fa0052`.

---

## Frontend

Files modified

- None this session (verification only).

Components / Services

- Verified the deployed bundle; no source change.

Issues

- Two `main-*` bundles observed in the served output (`main-TOB5U3OY.js`, `main-3QDBDHMX.js`); server/CDN cache should be cleared so `index.html` serves one consistent Sprint-007 build.

---

## Backend

Files modified

- None this session (deployment/verification only).

Controllers / Services

- No code change; schema was the gap.

Database

- **Migrations applied to `rbttaces_api`** (by the operator):
  - Created `metric_type_accounts`, `achievements`, `achievement_evidence`.
  - Added the additive `gps_target_metrics` measurement columns.
  - Seeded **5** revenue bindings.
  - Added unique functional index `uq_evidence_metric_snapshot`.

---

## Decisions

- Production configuration is confirmed correct; the failure was schema, not paths/CORS/API base.
- No "real" verified production achievement was created for testing (its snapshot is immutable) — verification used read-only endpoints and a non-existent id.

---

## Production verification evidence (live)

| Check | Result |
| --- | --- |
| `gps-targets/measures.php` | `200` — REVENUE_TOTAL, REVENUE_EXPORT, REVENUE_ANNUAL |
| `gps-targets/measures.php?company_id=99` | `200` — each `{account_count:0, usable:false}` (reported, not fabricated) |
| `metric-type-accounts/list.php` | `200` — **5 seed bindings** (REVENUE_TOTAL→domestic+export, REVENUE_EXPORT→export, REVENUE_ANNUAL→domestic+export) |
| `achievements/counts.php?company_id=99` | `200` — `{total:0, by_status{...}, decisions:0}` |
| `achievements/list.php?company_id=99` | `200 []` |
| `achievements/awaiting-review.php?company_id=99` | `200 []` |
| `gps-targets/measure-preview.php` (REVENUE_TOTAL / REVENUE_EXPORT, FY7/FY1) | `200` — full measurement contract, `calculation_version:"rev1"`, `status:"no_accounts"` for company 99 |
| `achievements/verify.php` (id 999999) | `404 {"error":"achievements id 999999 not found"}` (DB reachable, no 500) |
| Companies | 131 |
| Financial years | 10 (active FY 2024/2025) |
| `gps_target_metrics` (prod) | 0 |

Before → after: `SQLSTATE[42S02] table doesn't exist` → `200`.

---

## Outstanding

- Production **browser smoke**: `results`, `gps-targets-v2` (target popup: separate task/outcome progress + derived actual), `company/99/financial-indicators` → create revenue target. Not run this session.
- Prod **Director** login is not `Test123!` (that was local-only), so the live Director-denial/isolation check was not run.
- Company 99 has no account bindings → measures read `no_accounts` until admin maps accounts.
- Whole-folder drop also placed non-production artifacts in the web root (0-byte PHP: `category/ping.php`, `test-endpoint.php`, `debug-bulk-attach.php`, `bulk-attach-companies.php`, `node/add-form-definition.php`; dev scripts `companies/migrate.php`, `*/test-*.php`, `imports/normalized-migrate*.php`; data/docs `imports/*.json`, `grant/*.data.json`, `*.md`, `upload/**/*.png`) — should be removed/denied.
- Serve one consistent Angular build (clear cache).

---

## Verification

- Live authenticated calls (SA `mrnnmthembu@gmail.com`, id 77, company 99) against `https://app.rbttacesd.co.za/api/api-nodes/...`.
- Deployed `Constants` matched `src/services/service.ts`.
- Schema gap fixed; all new endpoints `200`.

---

## Next Session

Run the production browser smoke (both entry paths + Results/Awaiting Review/popup/modal/persistence, zero unexpected post-login console errors), clear the Angular cache so one build is served, then finalize the production rollout report and the artifact cleanup list.
