# Session 029

Date:
2026-09-23

## Goal

Prepare the consolidated Incubator OS **production deployment package** for Sprint-007 (Results/Achievements), Sprint-008 (Calendar) and Sprint-009 (Sessions). Prepare and verify only - **no production action**, no connection to or inspection of the production server, no probing of protected files. End at an explicit hold-point until user-provided preflight results are reviewed.

---

## Completed

- Inspected the real repository + Git history and confirmed exact ranges: 007 `83d6351..2fa0052` (14 commits), 008 `34a0dd5..6420d11` (2 commits), 009 `6420d11..2732e3b` (1 commit).
- Enumerated the union file set: **124 backend paths** (111 A, 11 M, 2 D) and **34 frontend source paths**; identified files touched by more than one sprint (last-touch wins).
- Resolved production path mapping against already-deployed capability features: `api-incubator-os/<X>` → `/api/<X>`; so `api-incubator-os/api/calendar/...` → `/api/api/calendar/...` is **correct**; the `/api/api/` rule means "do not nest the whole repo", not "avoid the double api in repo paths that already contain `api/`".
- Verified the four migrations by SQL inspection (idempotency, ordering, dependencies) and re-ran all four locally: exit 0, no error.
- Proved no migration registry exists; preflight infers state from `information_schema`.
- Confirmed the two deleted files (`models/MetricRecord.php`, `api-nodes/enhanced-metrics.php`) have **no executable consumer** (repo-wide search).
- Confirmed the never-upload list and that **no** config/secret/local artifact appears in the manifest.
- Wrote `docs/sprint-007-009-production-deployment.md` (dependency chain, MySQL requirements, 14-step manual procedure, hold-point, hard stops).
- Wrote `docs/deployment/preflight-readonly.sql` - read-only, self-guarded; **tested on both a migrated DB (all four `PRESENT`) and a fresh pre-007 DB (all four `MISSING`), no SQL errors**.
- Wrote `docs/deployment/post-migration-integrity.sql` - runs clean locally; all invariants 0 rows.
- Generated `docs/deployment/backend-manifest-sha256.md` (117 deployable rows, layered 1-7, per-file SHA-256, complete vs Git diff, 3/3 random SHA spot-checks MATCH).
- Generated `docs/deployment/angular-manifest-sha256.md` (production build output, 73 files, `index.html` + `main-*.js` SHA).
- Wrote `docs/deployment/smoke-tests.md` (auth, 007, 008, 009, integrity, evidence capture) and `docs/deployment/rollback-matrix.md` (per-failure-point, critical immutable-snapshot rule, pre-write drop order).
- Ran local verification: PHP lint **103/103**, sessions **105/105**, calendar **65/65**, calendar+session unit **20/20**, production build clean (sessions 55.4 kB / calendar 36.5 kB chunks).

---

## Frontend

Files modified

- None (deployment package is documentation + SQL only).

Components / Services

- None.

Issues

- The build output carries three **pre-existing** `public/` fixtures (`monthly-revenue-preview.html`, `test-integration.html`, `tableConvert.com_f92q5m.json`). Not part of this release; flagged in the Angular manifest, harmless, review before upload.
- 9 unrelated baseline Angular failures (standalone components in `declarations`) remain; documented as baseline and left untouched.

---

## Backend

Files modified

- None (deployment package is documentation + SQL only).

Controllers / Services

- None.

Database

- No schema change. Four migrations re-verified idempotent locally.

---

## Decisions

- **Deployment is manual and human-run** (phpMyAdmin + FileZilla); this package contains no production commands.
- **Hard hold-point after preflight.** No migration/file is approved until the returned grids are reviewed. `PARTIAL - STOP` and `PRESENT BUT INVALID - STOP` are hard stops.
- **Do not assume Sprint-007 is deployed.** The preflight re-measures live state rather than trusting the 2026-09-14/16 record.
- **Measure, do not assume, the Sprint-007 limitations.** The ~105 unresolved rows and export-account coverage are measured live in preflight Section E.
- **Angular deployed last**, only after migrations verify, backend is present, and auth/Calendar/Sessions smoke tests pass.
- **Tables are not dropped after live writes.** Rollback defaults to file/bundle restore + forward-fix; verified Achievement snapshots are irreversible history.
- Preflight is self-guarded so it cannot abort on a not-yet-created table.

---

## Outstanding

- **Production preflight results** (the hold-point). Nothing is approved until these are returned.
- Production deployment execution (out of scope for this session).
- Serve one consistent Angular build on prod (two `main-*` bundles were observed historically).
- Prod artifact cleanup (whole-folder drop residue) - documented previously, not sprint-specific.

---

## Addendum - production copy inspection + phpMyAdmin single-result fix

The operator reported that running the multi-statement preflight in phpMyAdmin showed **no results**
(phpMyAdmin's SQL tab only renders the **last** statement's grid) and instead supplied an exported
production database (`rbttaces_api (14).sql`, MySQL **8.0.46-cll-lve**). It was inspected **locally**
in throwaway scratch databases - no production server was touched.

Findings (measured, not assumed):

* **007a PRESENT**, **007b PRESENT**, **008 MISSING**, **009 MISSING** → apply 008 then 009, skip 007.
* MySQL 8.0.46 → 8.0.16+ requirement met.
* `unresolved_account_rows` = **100** across **62** companies (historical ~105 was approximate);
  `export_account_rows` = **0** → revenue non-authoritative for combined totals, unrepaired.
* Seeded bindings = 5. Achievements = 1 (company 120, unverified). Evidence = 0 → **no verified
  snapshot exists**, so 007 rollback is still clean. No collisions. Production 007 DDL matches the
  migration exactly (no drift).
* **Dry run on the scratch production copy**: applied 008 + 009 → exit 0, idempotent on re-run, all
  post-migration integrity invariants 0.

Fixes delivered:

* `docs/deployment/preflight-summary-readonly.sql` - a **single `UNION ALL` statement** (one grid)
  with a `J. VERDICT` action per migration. Tested on both the unmigrated and migrated production
  copy; no SQL errors.
* `docs/deployment/post-migration-integrity-summary.sql` - single-grid equivalent of the integrity
  checks. Tested on the migrated copy; all invariants 0.
* Deployment doc updated to make the single-grid files the primary operator path.

Scratch databases `prod_copy_check` / `prod_copy_fresh` are local throwaways and are dropped after use.

---

## Addendum 2 - live production preflight RECEIVED AND ACCEPTED

The operator ran `preflight-summary-readonly.sql` in phpMyAdmin against production `rbttaces_api`
(39 rows, one grid). Result:

| Migration | Live | Decision |
|---|---|---|
| 007a | **PRESENT** | SKIP |
| 007b | **PRESENT** | SKIP |
| 008 | **MISSING** | **APPLY** |
| 009 | **MISSING** | APPLY after 008 |

`any_partial_blocker = NO`; MySQL `8.0.46-cll-lve` (8.0.16+ OK); `calendar_before_sessions = OK`;
collisions 0. Readiness: unresolved rows **100** / **62** companies, export accounts **0**.
Bindings 5. `achievements` **0**, `achievement_evidence` **0** -> **no verified snapshot in
production**; 007 rollback stays clean.

Benign drift: the export showed `achievements` = 1 (unverified); live shows 0 - it was removed in
normal use, had no evidence, changes nothing.

**Outcome:** preflight hold-point cleared; approved actions are apply 008 then 009 (skip 007), then
backend upload. **No production action was taken by the agent** - the manual execution in section 4
remains with the operator.

---

## Next Session

Review the user-provided production preflight grids. If clean, apply only the `MISSING` migrations in locked order (007a → 007b → 008 → 009), verify each, upload backend Layers 1-7, run auth smoke tests, deploy the Angular build, then run the full smoke-test checklist and record evidence.
