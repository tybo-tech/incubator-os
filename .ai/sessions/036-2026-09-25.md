# Session 036

Date:
2026-09-25

## Goal

Planning session (no production code). Ingest three on-site visit / needs-analysis source documents
(`DS_Corp_Onsite_Visit_Report_1`, `DIAGNOSTIC ASSESSMENT & NEEDS ANALYSIS`, `Needs Analysis`), map them against
the existing Incubator OS data model, and produce the delivery plan: a discovery/standardisation document plus
two implementation sprint specifications (Site Visits, Needs Analysis).

---

## Completed

- Read all three source documents (two `.docx` extracted by unzipping `word/document.xml`; the scanned
  `Needs Analysis` PDF rendered to images and read visually — it has no text layer).
- Mapped the existing model (Company, Sessions, Calendar, Google Calendar, normalized SWOT/GPS,
  Results/Achievements, Categories/Cohorts, funding nodes) against every field in the three documents.
- Identified the reuse surface and the gaps; recorded the duplication register (two purchase models, two form
  systems, `consolidated_assessment` progress defect, `session_entity_links` name/alias drift,
  `gps_target_sources.source_type='assessment'` with no assessment id).
- Locked canonical ownership rules (Company owns current facts; Calendar owns schedule; Sessions own the
  conversation; tasks are the execution layer; assessments observe, the profile owns; snapshots freeze truth).
- Confirmed three planning decisions with the user: **Discovery doc + Sprint 011 + Sprint 012**;
  **site visits extend Sessions** with a `site_visit` type; **needs analysis uses a new normalized
  assessment capability**.
- Wrote `.ai/sprints/Discovery-011-012.md` — field-by-field mapping (D1/D2/D3 → canonical source → action),
  duplication register, canonical ownership rules, and the visit/assessment/snapshot contracts.
- Wrote `.ai/sprints/Sprint-011.md` — Site Visits: 6 phases, domain model, invariants, API contract, routes,
  target file structure, out-of-scope, future modules, definition of done.
- Wrote `.ai/sprints/Sprint-012.md` — Needs Analysis: versioned framework, scoring contract, traceability,
  6 phases, domain model, invariants, API contract, routes, target file structure, definition of done.

---

## Frontend

Files modified

- None (planning only).

Components

- None.

Services

- None.

Issues

- None.

---

## Backend

Files modified

- None (planning only).

Controllers

- None.

Services

- None.

Database

- None. Planned migrations: run order **#26** (site visits) and **#27** (assessments framework +
  GPS assessment provenance).

---

## Decisions

- **Two delivery sprints, preceded by discovery** — packaged as three `.ai/sprints/` documents; discovery has
  its own exit criteria so no implementation begins on assumptions.
- **A site visit is a Session.** Add `session_type='site_visit'` and attach a structured report
  (`session_visit_reports` + items + sign-offs + versions) inside `capabilities/sessions`. Reuse facilitator,
  participants, attendance, calendar event, Google Meet and `session_entity_links`; invent nothing.
- **Needs Analysis becomes a first-class normalized capability** (`capabilities/assessments`), not a node or a
  form. Framework is versioned data; scoring is a pure service; finalisation writes an immutable version.
- **Interventions link outward.** Agreed actions are `gps_target_tasks`; outcomes reuse Achievements; the
  assessment never mutates those domains.
- **Provenance is fixed.** `gps_target_sources` gains real `assessment_id` + `assessment_finding_id` columns so
  `source_type='assessment'` stops being a label with no record.
- **Missing ≠ not applicable.** The score is a number and applicability is a separate flag; the denominator
  counts applicable weighted criteria only. Legacy `consolidated_assessment` (>100% progress) is flagged but
  not migrated.
- **Snapshots freeze truth.** Issued/finalised documents preserve exactly what was rendered; live edits never
  rewrite history; `incubator`-visibility notes are excluded from every snapshot and export.
- **Funding stays four separate states** (committed / paid / delivered / utilised) and is derived on read,
  never stored on a report.

---

## Outstanding

- **Discovery is not signed off yet.** Sprint 011 and 012 must not begin until
  `.ai/sprints/Discovery-011-012.md` exit criteria pass (confirm enrolment rule for DS Corp, confirm the
  funding projection sources, finalise the `assessment_criteria` seed list, reconcile link entity names).
- Sprint-011 and Sprint-012 are **drafts**; phase tasks are unchecked by design until discovery completes.
- The legacy assessment/questionnaire code and `consolidated_assessment` nodes are intentionally untouched
  (reserved migration).
- No production code, migration or endpoint was created or applied in this session.

---

## Next Session

Complete the **Discovery-011-012** tasks (D.1–D.9), sign off the ownership rules and contracts, then begin
**Sprint 011 Phase 1** (migration #26: `site_visit` enum + visit report tables).
