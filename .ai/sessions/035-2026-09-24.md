# Session 035

Date:
2026-09-24

## Goal

Implement **Sprint-010 Phase 6 only** — verification, documentation and the deployment package — and lock the **central-account operating model** into the docs. Cover the complete verification matrix, Google Cloud configuration, OAuth Production/verification guidance, the migration/file deployment package, rollback, production smoke tests, secret placement, the seven-day Testing-token warning, and the removal of the exposed plaintext credential. Stop after the verified commit.

---

## Completed

- Added `api-incubator-os/tests/GoogleCalendar.ps1` — the combined **offline** runner. It refuses to run unless the fake is enabled, aggregates the 5 service suites + 4 HTTP suites, and totals **488/488** with no network.
- Wrote `docs/google-calendar-api.md` — endpoints, the **connection ownership model**, sync states, error codes, the all-day exclusivity rule, the security contract, migrations, rollback and reserved future modules.
- Wrote `docs/google-cloud-console-setup.md` — OAuth client type **Web application**, the exact redirect URIs, the scope set, **Testing** mode + test users, the **7-day refresh-token expiry**, the **sensitive-scope verification** path and lead time, and credential placement.
- Wrote `docs/sprint-010-google-calendar-deployment.md` — the manual runbook: central-account model, what is deployed, dependency chain, preflight, the pre-existing files edited additively, the ordered steps, the FileZilla manifest, rollback, hard-stops and hold-point status.
- Added `docs/deployment/verify-google-compact.sql` — a single-row, read-only verifier with token-integrity invariants; local run returns one clean row, invariants 0.
- Extended `docs/deployment/backend-manifest-sha256.md` with section A2 (51 deployable files + SHA-256), `docs/deployment/filezilla-upload-checklist.md` with the Sprint-010 layers, `docs/deployment/rollback-matrix.md` with §9 (Google), and `docs/deployment/smoke-tests.md` with §G (G0–G12).
- Updated `docs/calendar-api.md` with the Google projection section and cross-links.
- Locked the central-account operating model into `google-calendar-api.md`, the deployment runbook and the sprint.
- Removed the exposed plaintext credential from the gitignored `docs/0001notes.md` and recorded the required rotate + 2FA operator actions. Verified the value appears in **no** tracked file and was never committed.
- Updated `.ai/sprints/Sprint-010.md` (Phase 6 tasks/exit criteria/evidence, status header, file tree, Definition of Done).

---

## Frontend

Files modified

- None this phase (Phase 5 delivered the Angular surface).

Components

- None.

Services

- None.

Issues

- None.

---

## Backend

Files modified

- `api-incubator-os/tests/GoogleCalendar.ps1` (new — combined offline runner).
- `api-incubator-os/migrations/README.md` (verified rows #22–#25 are present; no change needed this phase).

Controllers

- None.

Services

- None.

Database

- None (no migration change this phase).

---

## Evidence

| # | Acceptance item | Result |
| --- | --- | --- |
| 1 | Combined offline runner | PASS — `tests/GoogleCalendar.ps1` **488/488**, no network |
| 2 | API reference complete | PASS — every endpoint, state, error code, security contract |
| 3 | Cloud Console + verification guidance | PASS — Web app client, redirect URIs, scopes, Testing + 7-day warning, verification lead time |
| 4 | Deployment package (migrations #22–#25 + files) | PASS — runbook + 51-file manifest + checklist |
| 5 | Secret placement | PASS — `config/google.local.php` operator-only; committed loader only; fails closed |
| 6 | Rollback | PASS — §9: revoke tokens, optional Google-event delete, pre-write drop only |
| 7 | Production smoke tests | PASS — section G0–G12 |
| 8 | Central-account model locked | PASS — one owner, OAuth-only, no password, view-not-manage, future tenant-owned multi-user |
| 9 | Exposed credential removed | PASS — removed locally; rotate + 2FA documented; not in any tracked file |
| 10 | Verifier | PASS — one clean row; token/cipher invariants 0 |
| 11 | PHP lint | PASS — 620/620 |
| 12 | Production build | PASS — clean, no new budget warning |
| 13 | Phase 1–5 regressions | PASS — 62 / 73+23 / 108+26 / 92+33 / 34+37 |
| 14 | Clean commit + push | see below |

---

## Decisions

- **One combined runner, not one more suite.** `GoogleCalendar.ps1` orchestrates the existing suites, refuses to run without the fake, and is the single sprint-level proof. It never contacts Google.
- **The ownership model is documented before deployment.** The central-account model is written into the API reference, the console runbook and the deployment runbook so nobody introduces multi-user publishing ad hoc.
- **Multi-user publishing is explicitly future work.** It is a **tenant-owned connection** with its own migration and authorization model — recorded in "Reserved future modules" and the deployment runbook, not built now.
- **A single-row verifier.** `verify-google-compact.sql` follows the proven compact pattern (one `SELECT`, no `UNION ALL`) so it renders reliably on the production phpMyAdmin; the token invariant checks that a cipher never exists without its nonce/tag.
- **The deployment runbook treats the eight edited pre-existing files as a distinct layer.** They are additive, post-commit, no-op-defaulted hooks; rollback is a plain file restore.
- **The exposed credential was removed from disk but the rotation is an operator action.** Incubator OS cannot rotate a Google password; the doc states exactly what the operator must do (rotate + 2FA) and never stores the value.

---

## Outstanding

- The **manual E2E with a real Google test account** is the operator's deployment hold-point step (needs real credentials + a registered OAuth client). The offline suites and the smoke-test checklist cover all logic in advance.
- The preflight for migrations #22–#25 has **not** been run against production; the runbook is ready.
- The 9 pre-existing unrelated Angular spec failures remain (chart/shell components), unchanged.

---

## Next Session

Sprint 010 is complete. The next work is the **operator-run deployment** of Sprint 010 (follow `docs/sprint-010-google-calendar-deployment.md`) or, if the product direction changes, the **future tenant-owned Google connection** enhancement — scoped as its own sprint with a migration, authorization model and consent, never folded into a deployment.
